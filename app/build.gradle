plugins {
    id 'com.android.application'
}

android {
    namespace 'com.bubbleladder.triplehedgev3'
    compileSdk 35

    defaultConfig {
        applicationId "com.bubbleladder.triplehedgev3"
        minSdk 24
        targetSdk 34
        versionCode 2
        versionName "3.1-sample-guard"
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    signingConfigs {
        stable {
            storeFile file("triplehedge-stable.jks")
            storePassword "TripleHedge2026!"
            keyAlias "triplehedge"
            keyPassword "TripleHedge2026!"
        }
    }

    buildTypes {
        debug { signingConfig signingConfigs.stable }
        release {
            signingConfig signingConfigs.stable
            minifyEnabled false
        }
    }
}
